# Things to do - create detailed md files for each of them

# Adding Compression

Add gzip compression to requests

# Remove explicit directory listing

To get a more git-like sync behavior let's get rid of all the adding of directories to the checksums, all the adding "dir" as a pseudo hashvalue and the "/" at the end of paths to direcotries

# Already done
